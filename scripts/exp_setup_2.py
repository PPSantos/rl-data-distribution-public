"""
    Experimental setup 2: 
        (1) first run scripts/sampling_dist_from_q_vals.py to generate a
        set of sampling distributions.
        (2) then run scripts/train.py by setting the custom_sampling_dist
        to one of the sampling dists calculated in (1).
"""
pass